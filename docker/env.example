# Copiar a .env en esta misma carpeta (docker/.env):
#   copy env.example .env   (Windows, desde docker/)
# Ajusta contraseñas en entornos reales; no subas .env a Git.

POSTGRES_DB=microrm
POSTGRES_USER=microrm
POSTGRES_PASSWORD=microrm_dev
POSTGRES_PORT=5432
